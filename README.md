# uuidclip

Converts clipboard content from ObjectId to UUID representation or vice versa. The clipboard content is only changed if the content matches either an ObjectId or UUID.

## Keybind
`Ctrl + shift + .` to toggle between ObjectId and UUID